# Renderer, geometry, foundation and generated assets

Audit date: 2026-09-25. This review describes the files present in the working tree, including uncommitted work. It does not certify the whole game as ported. Original-game assets, recovered shader programs and captured evidence remain in ignored `.local/` directories.

Status meanings in this document:

- **Pending**: no executable implementation of the named feature was found.
- **Infancy**: a scaffold, approximation or narrowly exercised prototype exists.
- **Mid-stage**: useful implementation and tests exist, with important coverage or integration gaps.
- **Fully ported**: the explicitly bounded feature is implemented and has relevant verification. This does not extend to adjacent Unity features or all assets. Native infrastructure is identified as such; completion of a native helper is not evidence of original-game parity.

## Critical boundary: production rendering versus diagnostics

The running Maker/Studio renderer uses `Renderer.swift`, `Pipelines.swift` and `Shaders/Toon.metal`/`Outline.metal`/`Post.metal`. These implement the native toon style, selected recovered material-composition controls, and the normal live deformation path. They do **not** choose recovered Unity shader programs by source shader identity.

`OriginalFrameProbe`, `OriginalFrameDiagnostics` and `TranslatedSourceShaderProbe` are separate, synchronous validation paths. They render the original player's already evaluated, frozen geometry with matched camera/material inputs. Their results establish raster/shader-input behavior for that fixture, not native animation, rig, card loading, Studio behavior or live renderer shader parity. The translated probe now runs all eight visible shader families in the controlled character fixture, but its full-character result still fails the comparison gate.

## Feature inventory

### Math and coordinate conversion

Files: `Packages/Engine/Sources/CoreMath/{Transform,Quaternion,Projection,Ray,UnityCoordinates}.swift`; tests in `Packages/Engine/Tests/CoreMathTests/`.

| Feature | Status | Specific observations and comments | Remaining work |
|---|---|---|---|
| TRS composition, transform points/directions, inverse-transpose normals | Fully ported — bounded native math | Column-major `translation * rotation * scale`; normal matrices use inverse transpose. These helpers assume usable finite input rather than validating every call. | Retain these conventions at all import boundaries; add singular-input handling at public editing/import seams where absent. |
| Native XYZ quaternion/Euler conversion and shortest rotation/look rotation | Mid-stage | Native XYZ is distinct from Unity Z-X-Y. `rotationQuaternion` explicitly assumes no shear and removes scale by unsigned column lengths; it must not be used to preserve reflections or arbitrary affine transforms. Zero-vector look/rotation inputs can normalize to nonfinite values. | R4: make invalid-input contracts explicit and test reflection/shear and degenerate direction callers. |
| Finite/infinite perspective and orthographic reverse-Z | Fully ported — bounded native projection | Right-handed, Y-up, Metal depth `[0,1]`, near→1. Core tests verify perspective depth mapping. | Broaden orthographic/extreme-frustum tests before claiming unrestricted camera import. |
| Unity position, direction, normal, tangent, quaternion, axial-vector and affine basis conversion | Fully ported — specified basis change | `UnityCoordinates` reflects Z once; tangent handedness and triangle winding change too. Matrices use `C * M * C`, preserving signed scales and shear. No unit, UV or color conversion is implied. Source Euler export retains equivalent Z-X-Y orientation rather than original textual angles. | Keep raw Unity versus converted glTF provenance explicit; use existing hierarchy/skinning/round-trip tests for new importers. |
| Rays, AABB construction/transformation and primitive intersections | Mid-stage | Slab, plane, triangle, sphere, segment and point-distance helpers exist. `AABB.isEmpty` tests X ordering; matrix decomposition and degenerate rays are not a general validated geometry API. | Add focused regression cases for zero directions, nonuniform transforms and empty/degenerate boxes at actual picking call sites. |

### Scene rigs, avatars, bounds, cameras and lights

Files: `Packages/Engine/Sources/Scene/{Skeleton,Rig,SourceRig,SourceAvatar,SourceRigBounds,Camera,Light}.swift`. IK solver behavior in `IK.swift` and `SourceTrigonometricIK.swift` is covered by the Studio/animation audit, not by this section.

| Feature | Status | Specific observations and comments | Remaining work |
|---|---|---|---|
| Legacy/native `Skeleton`, `Pose`, `PoseDelta` | Mid-stage | Name-keyed bones, parent-first evaluation, TRS, nonpropagating skin scale and pose blending support generated assets. `Skeleton.from` folds non-joint ancestors into root joints, but it is not the full source-hierarchy path; duplicate names and malformed direct construction are unsafe assumptions. | R4: validate direct construction; preserve intermediate non-joint transforms or explicitly constrain legacy imports. |
| Source hierarchy and renderer-specific skin palettes | Fully ported — validated interchange contract | `RigDefinition` retains support transforms, stable source IDs, duplicate display names, authored affine matrices, active hierarchy flags and per-renderer inverse binds. Evaluation is mesh-local `inverse(meshWorld) * boneWorld * bind`. Cycles, bad references, nonfinite matrices and singular mesh transforms fail. | Expand original-asset corpus; do not route source rigs through the legacy name-keyed skeleton. |
| Source mesh decoding and four-influence CPU reference skinning | Fully ported — schema-1 subset | `SourceRig` converts raw Unity coordinates/winding once, checks palette indices including zero-weight lanes, normalized nonnegative weights, finite streams and selected submesh geometry. UV0/1/2 are retained. CPU deformation is available independently of GPU rendering. | R3: broader source-format support below is still missing. |
| Source blendshape loading | Mid-stage | Sparse position/normal frames are supported only for unique single-frame channels at source weight 100; expression weights resolve later. Nonzero initial weights, multiframes and nonzero tangent deltas are rejected, not reconstructed. | R3: recover original interpolation and initial-weight semantics, add tangent deltas and compare CPU/GPU output. |
| Body/head/clothes/hair assembly and accessory rebind | Mid-stage | `SourceAvatar` reproduces observed parenting, local-transform copies and palette rebinding without rewriting inverse binds/vertex joint indices. Manifest carries sex, head ID and bone type; selected source variants are handled upstream. Names must uniquely resolve within component masters; accessories enforce the original 20-slot bound. | R3: support currently rejected source component types; additional installed variant coverage belongs to Maker audit. |
| Unity Cloth in source assemblies | Pending | `hasCloth` is detected and `SourceAvatar` rejects selected cloth parts. Detection is not simulation or usable import. | Recover cloth parameters/bind constraints, implement or explicitly separate a verified static preview mode, then test deformed garments against original captures. |
| Exact CPU bounds | Fully ported — supported source meshes | `SourceRig.bounds` computes deformed positions for active/renderable parts; useful for initial camera fit and independent oracles. | Retain as reference; avoid restoring it as an every-frame full-vertex cost. |
| Conservative live bounds | Fully ported — supported normalized linear skinning | `SourceRigBounds` caches per-joint boxes after morphs, transforms and unions them each pose; nonnegative normalized weights give containment. Includes reflection/shear handling and floating-point padding. This is intentionally conservative and can enlarge a shadow fit. | Test newly introduced deformation types against exact bounds; invalidate cache if geometry/morph identity becomes mutable. |
| Orbit camera, imported roll override, ray/project helpers | Mid-stage | Shared Maker/Studio camera supports orbit, pan, dolly, infinite reverse-Z perspective, and an optional orientation quaternion retaining imported roll. Orbit editing clears that override. | R4: test imported camera semantics across roll, lens/frustum types, orbit edits and round-trip export; orthographic viewport camera is absent. |
| Directional/point/spot light data | Mid-stage | `SceneLight` carries kind, color/intensity, range, cone, enabled and `castsShadow`. Main light has a separate camera-relative direction function; code follows camera yaw, despite a comment mentioning yaw+pitch. | R1: original light transform/attenuation comparisons; per-light shadow flags currently exceed renderer capabilities. |

### glTF/GLB import and asset data

Files: `Packages/Engine/Sources/Assets/{AssetModel,GLTFSchema,GLTFValidation,GLBLoader}.swift`. `JSONValue`, MessagePack and source mod package/catalog/profile files are covered by the character/mod audit.

| Feature | Status | Specific observations and comments | Remaining work |
|---|---|---|---|
| GLB 2.0 container and glTF JSON/external/base64 buffers/images | Mid-stage | Chunk order/length/alignment, declared buffer bounds, graph references and image sources are checked. External paths resolve relative to the asset. This general loader does not itself impose package containment or symlink policy. | R5: keep package containment checks before this API; make trusted standalone-file versus packaged-mod loading modes explicit. |
| Accessors and mesh topology | Fully ported — declared subset | Normalized integer/float streams, byte stride/offset, sparse accessors, exact UInt32 indices and triangle lists are implemented with finite/range checks. Cycles/multiple parents/invalid roots fail. MAT2/MAT3 and non-triangle primitives are rejected. | Expand only when real source/mod requirements demand it; preserve strict rejection for unsupported required data. |
| Node hierarchy and static affine matrices | Fully ported — static imported graph | `NodeData.authoredMatrix` avoids losing reflection/shear; `meshNodes` traverses only active glTF scene roots. | Verify all render consumers use `localMatrix`, not decomposed TRS, for static assets. |
| Mesh normals, tangents, colors and outline smoothing | Mid-stage | Missing normals are generated; absent tangents receive a fallback downstream. `MeshUtil.smoothNormals` welds coincident positions for outlines. Colors are quantized to bytes on GPU upload. | Use authored source outline channels/normal semantics where available; quantify color precision and tangent-generation requirements. |
| Skinning influence sets | Mid-stage | `JOINTS_0/WEIGHTS_0` are read; a second set is reduced from eight influences to the largest four and renormalized. This is intentionally lossy relative to an eight-weight asset. | R5: emit an explicit compatibility diagnostic or support eight influences and verify against the original asset. |
| Secondary UV streams | Mid-stage | `MeshData` and GPU storage support UV1/UV2, and `SourceRig` supplies them. **`GLBLoader` currently only reads TEXCOORD_0**, despite the wider data model. Material texture bindings require texcoord 0. | R5: decode/validate TEXCOORD_1/2, retain material texcoord selection and test independent iris/lightmap-style streams. |
| glTF materials | Mid-stage | Base color texture/factor, normal texture, emissive factor, alpha mode/cutoff and double-sidedness survive. Native toon material conversion uses these attributes. | R5: sampler/wrap, UV transforms, metallic/roughness/emissive textures and required extension handling are incomplete; expose supported subset. |
| glTF morphs | Mid-stage | POSITION/NORMAL deltas and target names load. Tangent deltas and authored initial mesh/node weights are not applied. | R3/R5: preserve initial weights and tangent morph semantics, with fixture tests. |
| glTF animation, cameras and extension-driven features | Pending | No glTF animation player/import into this asset model; source Animator uses a separate pipeline. Draco, meshopt, KTX/Basis and general extension execution are not implemented. Unknown JSON fields can be ignored by decoding; this is not proof they are supported. | Reject unhandled `extensionsRequired`, document extensions supported by native mod interchange and add only demanded decoders. |

### Metal resources, frame graph and native materials

Files: `Packages/Engine/Sources/GPU/{GPUContext,FrameRing}.swift`, `Packages/Engine/Sources/Renderer/{Renderer,Pipelines,RenderTargets,RenderFrame,ResourceStore,RenderBenchmark}.swift`, `Packages/Engine/Sources/ShaderTypes/{shim.c,include/ShaderTypes.h}`, and all seven `Shaders/*` files.

| Feature | Status | Specific observations and comments | Remaining work |
|---|---|---|---|
| Device/queue, three-frame pacing, bundled metallib and GPU capture hooks | Mid-stage | Native Metal context and semaphore control are real. One shared queue is used; precompiled `IkkokuShaders.metallib` is loaded explicitly. GPU capture failures are swallowed by the debug helper. | R6: stress interactive draws, capture and pick together; report allocation/command errors without silent frame loss. |
| Shared CPU/Metal ABI | Fully ported — current native declarations | All GPU-visible layouts/bind slots are in one C header; Swift tests assert key strides. This is a native ABI, not an IL2CPP struct-memory parity layer. | Add stride/offset assertions when new vertex/uniform structs are introduced. |
| Uniform ring | Mid-stage | 256-byte aligned, 8 MiB per in-flight region. Overflow uses a `precondition` and can terminate a large scene. | R6: preflight or grow/spill allocations; stress enough draw items/lights/gizmos to cross the configured region bound. |
| Mesh/texture handles and lifetime | Mid-stage | Locked resource store, stable handles, explicit mesh/texture removal and deformation-buffer release exist. Render-frame snapshots retain explicit textures across appearance replacement. Fallback white/black/normal textures are built in. | R6: test repeated character/scene replacement and allocation failure; hidden-buffer removal is not exposed like deformation-buffer removal. |
| Texture upload and sampling | Mid-stage | Production image uploads request generated mipmaps and sRGB flags. Source-authored mip recovery and original sampler behavior exist in the diagnostic path, not the general live-material path. | R1: preserve imported mip/sampler/color-space metadata in production materials. |
| GPU morph + linear-blend skinning | Fully ported — native four-weight/single-frame subset | Morphs run before skinning. Dynamic palettes are not capped at 256 bones; all lanes are checked. Kernel normalizes weights safely, uses inverse-transpose/cofactors for nonuniform/reflected normal transforms and preserves tangent sign. Invalid bindings fail captures and are skipped interactively. | R3: original normal/tangent semantics need per-asset parity validation; multi-frame/tangent morphs still absent. |
| Active morph budget | Mid-stage | `Renderer.prepare` filters small/nonfinite/out-of-range weights and takes at most `IK_MAX_ACTIVE_MORPHS` (64). More live targets are truncated without a dedicated overflow diagnostic. | R6: explicit overflow validation or chunked accumulation; test a character with more than 64 active meaningful targets. |
| Pass graph and target allocation | Mid-stage | Compute deform→main-light shadow→4×MSAA HDR toon/outline/transparent→bloom/grade/FXAA→overlay. Reverse-Z depth, memoryless MSAA attachments and resize reuse are implemented. | R1/R6: original pass-state parity, quality variants and representative scene stress remain open. |
| Native toon, skin, cloth and hair shading | Mid-stage | Albedo tint, color-mask zones, normal/detail/line textures, rim/specular, anisotropic-style hair effects, overlays and patterns work in the native shading model. This is visibly approximate relative to recovered original shaders. | R1: move verified source program/material contracts into live rendering, then validate all families and variants. |
| Source body masks and iris highlight channels | Mid-stage | Source RG coverage control and independent UV1/UV2 iris highlight composition are implemented across relevant passes. This does not reproduce the complete body/eye shader. | R1/R3: verify appearance variants and interaction with original stencil/outline/alpha behavior. |
| Outlines and culling | Mid-stage | Native screen-scaled extruded outline, smooth normals, per-vertex weight/color alpha, model-reflection front-face changes and material culling exist. Production depth targets have no source stencil buffer. | R1: original per-family stencil masks, pass ordering and outline geometry are diagnostic-only; integrate verified contracts. |
| Main light shadow | Mid-stage | One 2048² fitted map, PCF and bias; alpha/body-mask cutouts affect shadow pass. Fit uses scene bounds. Transparent items do not cast shadows in this path. | R1: source shadowcaster shaders, self-shadow bias/filter quality, moving/camera-relative lights and large-scene stability. |
| Additional lights and shadowing | Infancy for original parity | Up to eight additional directional/point/spot lights contribute native toon lighting. `SceneLight.castsShadow` does not produce per-light maps. Additional-light overflow is ignored after the limit. | Implement documented limit diagnostics and source attenuation/pass behavior; add extra-light shadows only after source requirements are recovered. |
| Transparency and draw order | Mid-stage | Transparent items sort by distance from model-transformed static mesh-bounds center; opaque ordering uses explicit order/material kind/mesh ID. Source render queues/stencil ordering are not production inputs. | R1: introduce source queue/pass ordering and evaluate intersecting hair/glass and animated bounds cases. |
| Post effects and background | Mid-stage native feature; Infancy for original parity | Five-level bloom, exposure/contrast/saturation/temperature, optional tone map, vignette, fog, FXAA, gradient/transparent background are real. Their formulas are native. `toneMappingEnabled=nil` preserves older native behavior. | Recover original effect-stack order and parameters; compare each effect independently, then combined. Do not mark parsed Studio effect settings fully rendered. |
| Grid/gizmos and picking | Mid-stage | Grid, depth-tested/overlay lines/triangles, integer object-ID picking and always-on-top pickable handles exist. Picking rerenders synchronously and reads back one pixel. | Validate exact alpha/coverage/visibility semantics against displayed objects; profile interaction stalls and consider deferred pick readback. |
| Still capture | Fully ported — bounded native capture | Offscreen capture accepts bounded dimensions, waits for completion, checks deformation/command failure and returns a CGImage. This is the basis of reproducible image fixtures. | Keep capture-only settings explicit so grid, gizmos, post effects and time match comparison fixtures. |
| Render benchmarking and resource counts | Mid-stage | Synchronous reused-target benchmark records CPU encoding, GPU duration, completion latency, RSS and Metal allocation plus item/triangle/resource counts. It excludes readback and simulation. | R7: interactive end-to-end frame timings, cold loads, scaling, long-running memory and corpus asset coverage. |
| Scene culling, LOD, streaming and large-scene batching | Pending | No production frustum/occlusion culling, source LOD selection, asset streaming budget or GPU-driven batching was found in this renderer. Items are iterated and drawn individually. | R6/R7: measure many-character/prop scenes, then add the highest-value source-compatible scalability work. |

### Original shader recovery and matched frame tooling

Files: `Tools/reverse/{source_shader_translation,original_character_probe,original_shader_probe,original_texture_mips,compare_original_frame,compare_original_pose,export_prefab,rig_inventory}.py`; `Tools/reverse/fixtures/{OriginalCharacterProbe,OriginalShaderProbe}.cs`; `Packages/Engine/Sources/IkkokuInspect/SourceCardPoseInspection.swift`; `Packages/Engine/Sources/Renderer/{OriginalFrameProbe,OriginalFrameDiagnostics,OriginalProbeTextureLoader,TranslatedSourceShaderProbe}.swift`.

| Feature | Status | Specific observations and comments | Remaining work |
|---|---|---|---|
| Static Unity prefab export | Mid-stage | `export_prefab.py` selects an explicit prefab and verified catalog mapping, emits geometry/material metadata and provenance, reflects geometry/winding/tangents/UV origin, and converts AG normal maps. It records unsupported components. | R5: inactive nodes/renderers, nonidentity texture transforms, animated items/alternate child roots, repeated material passes and broader shader families currently reject. Skinned meshes deliberately use the rig path. |
| Rig inventory/interchange extraction | Mid-stage | Full transform graph, source identities, palette audit, mesh attributes, sparse morph frames and metadata are extracted. Optional unused trailing bones can be omitted only with explicit validation/audit; referenced palette entries cannot be dropped. | R3: runtime still supports fewer morph forms than extractor; surface capability mismatch before conversion publishes a supposedly loadable asset. |
| Original Unity material-create pass probes | Mid-stage | Controlled original-player execution with exact input/provenance hashes and scoped process shutdown exists. Head/clothes compositor comparisons are separate from complete lighting. | Expand fixtures for all material recipe variants; keep compositor and final-frame gates separate. |
| Original character capture | Mid-stage | Fresh controlled clothed character fixture, original evaluated world geometry, UV streams, material properties/textures, camera/light/global parameters and image outputs. The probe player runs privately; original install is not rewritten. | R2: multiple controlled cards/poses/cameras/lights, including animated native pose inputs and source identity hashes. |
| Geometry/depth-normal comparison | Fully ported — one frozen fixture protocol; native pose gate partial | Identical image dimensions and source descriptor hash required; no alignment, recoloring or registration. Separate silhouette, depth, normals and color gates prevent a geometry pass being mistaken for visual parity. Diagnostic geometry intentionally ignores alpha, stencil and outlines. A native bone-transform gate now exists for one controlled fixture; it fails on 46 hand joints while the diagnostic excluding hands passes. | R2: retain the protocol while broadening fixtures and extend the pose gate to animated states and variants. |
| DXBC→MSL stage translation | Mid-stage | Strict bounded register translator preserves boolean bit masks, swizzles, destination lanes, `sincos` aliasing and UV orientation. It disassembles exact recovered DXBC and hashes stage/MSL identity. Unknown opcodes, dynamic constant indexing, loops and comparison sampling reject. Extraction selects the first subshader/subprogram of the requested pass, not every keyword/platform variant. | R1: recover variant selection and needed opcodes using original stage tests before widening acceptance. This is not a general CG/HLSL source compiler. |
| Translated source shader execution | Mid-stage | Diagnostic supports eight visible families and thirteen vertex/fragment pass pairs including five outlines. It binds original properties, matrices, UV0–3, source queues, supported stencil/blend states and sampler metadata. Unknown bindings/states fail. Source shadowcast is skipped because the fixture disables shadows. | R1: fix full-character mismatch; then cache pipeline objects and integrate live source materials. Current per-job compilation is unsuitable as a live frame implementation. |
| Source mip recovery | Mid-stage | Decoder accepts only unambiguous source identity with a base level matching original GPU readback; stores mip/provenance in derived metadata. Half-float linear pixels avoid 8-bit quantization. | R1: current latest comparison still uses generated mips for 37 textures; rerun authored-mip coverage and compare filter/anisotropy settings. |
| Matched live app versus running original | Pending beyond separate component oracles | Current whole-frame shader probe freezes the original pose. Separate rig/Animator tests exist, but this is not a complete end-to-end native scene/frame comparison suite. | R2: render both applications from the same card/scene/time/settings without substituting source-evaluated native vertices. |

### Procedural and bundled asset pipeline

Files: all `Tools/assets/` scripts and `Assets/{Characters,Hair,Clothes,Accessories,Items,Textures,Thumbs}/`, `Assets/catalog.json`. The full directory is covered; binary assets are inventoried rather than manually reverse engineered as code.

| Feature | Status | Specific observations and comments | Remaining work |
|---|---|---|---|
| Source/provenance and deterministic stage runner | Mid-stage | `Tools/assets/README.md` declares CC0 MakeHuman sources and procedural generation; source copies/licenses and intermediate data live in ignored `Tools/assets/source`/`out`. `run_all.sh` uses Blender factory startup and fixed generation stages. This audit reviewed that declaration/code, not an independent license provenance investigation. | R8: machine-readable input/output hashes, tool versions, dependency checks and clean rebuild validation. |
| Body/proxy generation | Mid-stage native fallback; Pending original equivalence | `build_body.py`, `landmarks.py`, `deform.py`, `rig.py` restyle MakeHuman bodies, fit eye/lash/brow/teeth/tongue proxies, build a 58-bone rig, 74 body morph targets, weights and region metadata. These are independently generated models, not recovered Koikatsu base bodies. | Keep fallback appearance/status explicit; original model fidelity must be assessed on the source avatar path. |
| Skin/face/eye/hair/pattern textures | Mid-stage native fallback | `textures.py` emits procedural AO/detail/face overlays, iris/highlights, lashes/brows, strand texture and five patterns. Source recovered material recipes are a different pipeline. | R8: catalog completeness, consistent color space and deterministic output verification. |
| Hair generation | Mid-stage native fallback | `build_hair.py` makes scalp/bangs/loft/tail/card styles and refits CC0 MakeHuman hair; long styles append hair chains. Catalog contains 16 styles. | Verify both-sex fit, expression/head-shape extremes and chain behavior; do not claim original hair asset coverage. |
| Clothing generation and coverage masks | Mid-stage native fallback | `build_clothes.py` combines refitted garments and generated shells/shoes/skirt; controlled layer offsets and regional weight donors; UV color masks plus body-UV coverage masks. Catalog contains 24 garments. | Verify morph extremes/pose coverage and openings; procedural mask estimates are not original wear-state semantics. |
| Accessories and Studio items | Mid-stage native fallback | `build_items.py` generates static meshes, attachment offsets and glass material hints. Catalog contains 8 accessories and 15 primitives/furniture/room items. | Verify parent transforms and transparent items; original Studio catalog/attachment reconstruction remains separate. |
| Catalog and thumbnails | Mid-stage | `catalog.py` builds IDs, file paths, masks, bones/morphs, defaults and texture lists using stage summaries/files. Present inventory: 2 bodies, 16 hair, 24 clothes, 8 accessories, 15 items; `Assets/` contains 205 files, including 63 thumbnails and 24 texture files. | R8: validate every catalog reference. Body entries name `.regions.bin` sidecars but none were present in the inventoried `Assets/Characters/` directory; runtime embedded `_REGION` handling and packaging should be checked explicitly. |
| Visual verification outputs | Mid-stage | `verify.py` produces Blender stills/contact sheets for bodies, rigs, morphs, hair, garments, accessories and rooms. Visual outputs do not by themselves prove Metal/native renderer behavior or source-game fidelity. | R8: regenerate in a clean environment, run native asset loading/thumbnail checks and retain build hashes; use clothed fixtures for shared image comparisons. |

## Persisted evidence and what it proves

Evidence is local and optional; these files must not become redistribution dependencies.

1. `.local/reverse/original-character-probe/frame-comparison.json`, source frame SHA-256 `9aa4de394acbae5e9a7d336b5990aa67f9619931dfce3cac6d1e8711c8bcc750`, reports:

   | Check | Persisted result | Status |
   |---|---|---|
   | Frozen geometry silhouette | IoU **0.9998195814**, 18 differing pixels | Pass against 0.999 gate |
   | Depth / encoded normals | p99 depth **0 m**, p99 normal channel difference **0 bytes** | Pass; nonzero edge maxima still present |
   | Production native toon baseline color | MAE **31.2589/255**, p99 **239** | Fail |
   | Translated opaque garments | IoU **0.9995455431**, color MAE **0.1330/255**, p99 **2** | Pass |
   | Full translated character | IoU **0.9988684583**, color MAE **26.9362/255**, p99 **242** | Fail |

   The color gate is mean ≤1 byte and p99 ≤4; there is no complete source visual parity claim. Latest file reports 55 textures, 18 without mips, 37 with generated mips, zero authored chains in this particular run. Thirteen shader program descriptors are persisted under `shaders/`; mere translation success is not a passed image comparison.

2. `.local/reverse/plugin-execution/release-capture-01/benchmark.json` is a selected live Studio fixture, 600×800, Apple M2 Ultra, 28 visible items, 60,761 triangles, 40,490 unique vertices, no missing registered draw meshes. It records separate measurements:

   | Measurement | p50 | p95 | Scope |
   |---|---:|---:|---|
   | CPU simulation/frame construction | 4.684 ms | 4.964 ms | 60 measured frames, 3 warmups; isolated Studio model, Animator/FK/IK/transforms/frame construction/one translated plugin |
   | GPU render execution | 0.529 ms | 0.537 ms | 20 reused-target synchronous offscreen samples |
   | CPU render encoding | 0.222 ms | 0.240 ms | Same offscreen render samples |
   | Render completion latency | 1.058 ms | 1.224 ms | Encoding through command completion |

   This fixture's report says **zero source DynamicBone components attached** and retains an unrendered route. It therefore does not benchmark hair dynamics or complete route rendering. RSS after capture is 379,125,760 bytes and Metal allocations 415,907,840 bytes; they overlap on unified memory and must not be added as independent usage. Cold load, readback, PNG encoding, display scheduling and whole-game asset coverage are excluded. These numbers are not an interactive FPS claim or a performance certification for all supported assets.

3. Relevant existing Swift test files: `CoreMathTests/*`, `GLTFTests.swift`, `GLTFImportTests.swift`, `MaterialImportTests.swift`, `RigTests.swift`, `SourceAvatarTests.swift`, `SourceMaleAvatarTests.swift`, `SourceRigBoundsTests.swift`, `SourceRigPreviewTests.swift`, `DeformationSafetyTests.swift`, `SourceMorphGPUTests.swift`, and appearance texture-lifetime tests. Metal tests require a Metal device. Some original-avatar tests return without exercising source assets if environment fixtures are absent; a green fixture-free run must not be reported as original-asset coverage.

4. During this documentation audit, **31 Python tests passed** using `.local/reverse/unitypy-venv/bin/python -m unittest test_source_shader_translation test_compare_original_frame test_export_prefab test_rig_inventory test_original_shader_probe -v` from `Tools/reverse/`. They cover translator rejection/bit-mask/lane/sample semantics, comparison gates, static geometry/material conversions, rig extraction validation and scoped probe lifecycle. No fresh original-player capture or Swift build was performed for this scoped documentation review.

## Actionable continuation queue

| ID / priority | Task and entry points | Acceptance criteria |
|---|---|---|
| **R1 — P1** | Finish original source shader parity. Start with per-family/pass isolation in `TranslatedSourceShaderProbe.swift` and `source_shader_translation.py`, then source material integration in `RenderFrame`/`Pipelines`/`Renderer`. | Full controlled character meets existing silhouette/color gates with hashes identifying exact programs, textures/mips, source globals and camera. Verify multiple head/hair/clothes/accessory families and actual keyword selection. Production Maker/Studio uses the same verified contracts; live animation captures pass without frozen source geometry. Unsupported programs remain explicit diagnostics. |
| **R2 — P1** | Partial: the native transform gate exists for one controlled clothed fixture. Add matched end-to-end scene/frame tests using controlled cards/scenes and fixed clocks. Extend `OriginalCharacterProbe.cs`, capture automation and `compare_original_frame.py`. | Recover hand patterns via ST-T07, then compare animated states, head/bone/sex and outfit variants, live scenes, and lights. Independently load original and native scenes, evaluate poses and render from matching inputs; compare transforms before geometry/color without source-baked vertex substitution. Cover camera roll, alpha/stencil and scene attachments with documented tolerances. |
| **R3 — P1** | Close source geometry/deformation omissions in `SourceRig`, `SourceAvatar`, extraction and GPU types/kernel. | Nonzero initial and multiframe blendshapes interpolate exactly; tangent deltas reach Metal; added deformation types preserve CPU/GPU parity and conservative-bound containment. Cloth is either implemented with original reference cases or rejected as a clearly named unsupported feature. |
| **R4 — P1** | Harden math/camera/legacy skeleton contracts. | Degenerate vectors, singular matrices and invalid bone graphs return a defined error rather than nonfinite state or traps. Imported signed/sheared transforms are preserved where supported. Camera round-trips retain roll/frustum settings until an intentional edit changes them. |
| **R5 — P1** | Complete the declared asset interchange subset in `GLBLoader`/`GLTFValidation`/`export_prefab.py`. | UV1/2 and requested material UV/sampler transforms survive; >4 weights, initial morph weights and required extensions are supported or explicitly rejected/reported. Static prefab visibility and child roots have exact metadata consumers. Package loads cannot escape verified package roots. |
| **R6 — P1** | Make renderer limits and resource lifetime recoverable. | Ring exhaustion, failed allocations and morph/light limits produce clear errors or supported growth, not termination or silent truncation. Repeated import/delete/capture/pick cycles show bounded resource counts. Concurrent interactive/capture submissions preserve deformation and uniform-buffer data. |
| **R7 — P1** | Establish performance and coverage budgets. Extend `RenderBenchmark` and app simulation reporting rather than inferring FPS from one probe. | Reproducible Release results for 1/multiple characters, many props, dynamics, transparency and effects; record CPU simulation, encoding, GPU, presented-frame intervals, p95/p99, cold load and memory growth. Report asset numerator/denominator by source catalog/category, converted/failed/unsupported counts and reason codes. |
| **R8 — P2** | Make fallback asset regeneration auditable and packaging complete. | Clean rebuild records input hashes/licenses, Blender/Python versions and output manifest; catalog references all resolve or are explicitly optional; embedded regions/sidecars agree; native load and clothed visual smoke captures pass for every generated asset family. Generated fallback assets remain visibly distinguishable from recovered source equivalents. |

## Coverage boundary

This page covers every file in `CoreMath`, `GPU`, `Renderer`, `ShaderTypes`, `Shaders`, the four named glTF asset files, the seven named Scene foundation files, all `Tools/assets` scripts and bundled asset categories, and the named geometry/shader/frame-comparison reverse tools and corresponding tests. Source card/material recipe resolution, mod archive identity, Maker UI, Studio serialization/IK/dynamics/animation/gameplay, general extraction orchestration, build/package shell and application lifecycle are documented in the other component audits. They are not silently rated complete here.
