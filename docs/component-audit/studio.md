# Studio component audit

Audit date: 2026-09-25. Scope: the working tree, including uncommitted implementation. This audit inspected every file in `Packages/Engine/Sources/Studio`, the app's `Studio` directory, the two Scene IK implementations, the associated tests, and the recovery tools listed below. It reviewed existing local measurements; it did not rerun the complete test suite. Original files and numeric/reference artifacts remain private under `.local/`.

Status means **Pending** (no native consumer), **Infancy** (initial data path or prototype), **Mid-stage** (substantial implementation with integration, coverage or parity gaps), or **Fully ported** (only the precisely named, bounded source behavior has implementation and source evidence). A native utility can be implementation-complete without being an original-game port. A parser or a retained byte field does not establish runtime support.

## Findings that change the completion assessment

- **ST-B01 — source inspectors are unreachable.** `Apps/IkkokuCreator/Studio/StudioView.swift:190` intercepts the source character's Pose, Face and Clothes tabs and displays the old statement that original full-body IK is not connected. `SourcePoseInspector`, thirteen-target editing callbacks and the source accessory-label view exist below that gate. Solver and deterministic capture tests therefore do not establish that a user can reach those controls.
- **ST-B02 — native Mute adapter startup trap (verified in PR #2 acceptance).** `StudioModel+Plugins.configureSourceMutePlugin(configuration:)` read `NSApp.isActive` while AppState initialization precedes creation of `NSApplication`; environment/capture mounts and `captureScene` saved-scene restores all reached it. The actual release launch with the native adapter failed with SIGTRAP; evidence is `.local/reverse/plugin-execution/native-adapter-capture-01` and `Ikkoku-2026-09-25-071053.ips`. The mount now defers the initial focus sample to `didFinishLaunching` when `NSApp` is nil (see ST-T02). The release probe now passes for installed-disabled and explicit-enabled configurations (head `93e6b06`, merged `aa4bcb9`); see the verified acceptance section in the native-adapter reference. The successful `release-capture-01` exercised the typed IR fixture, not this adapter mount.
- **ST-B03 — a loaded original scene is still a selective preview.** Source characters outside routes can render. Items, lights, object cameras and routes remain named folders in this import path; route descendants are deliberately unrendered. Current camera and ten camera slots are applied, while maps, original lighting/effects and scene audio remain parsed data.
- **ST-B04 — prototype controls are still exposed on source objects.** Object Inspector's Maker replacement, clothing/accessory visibility and native card controls write prototype fields. `SourceStudioCharacterPreview.frame` reads the source reference/record instead. Those fields can have no corresponding source visual effect and cause original-scene export to reject the scene. Removing ST-B01 alone would not implement source face/clothing editing.

## Feature inventory

### Documents, tree, native editor and capture

Code: `StudioDocument.swift`, `Gizmo.swift`, `Poses.swift`, `Timeline.swift`; `StudioModel.swift`, `StudioView.swift`, `StudioModel+Benchmark.swift`.

| ID | Feature | Status | Specific assessment and evidence | Next task |
| --- | --- | --- | --- | --- |
| ST-D01 | Native scene document and PNG scene-card persistence | Mid-stage | Codable objects, transforms, cards, ten camera slots, lights/effects, timeline, source references and plugin state persist. Original quaternions have a separate override until an Euler edit. Tests cover source references and old asset-reference compatibility. This is a native format, not full CharaStudio serialization. | ST-T03, ST-T15 |
| ST-D02 | Tree validation, ancestor transform/visibility, subtree deletion | Mid-stage | Native utility is implemented: duplicate UUID, missing parent and cycles reject; traversal has cycle guards and deleting a subtree removes its timeline keys. `StudioHierarchyTests` cover these operations. Full original tree/UI command semantics are not recovered. | ST-T15 |
| ST-D03 | Add/delete/duplicate/reparent, undo/redo | Mid-stage | Native editor operations exist, with 60 document snapshots and coalesced edits. UI duplicate copies one node; plugin Instantiate copies a subtree through a separate implementation. `setParent` uses document transforms, not the source attachment-aware frame, and decomposition does not reject shear. Source-key duplication can later make plugin binding ambiguous. | ST-T15 |
| ST-D04 | Pick IDs, translate/rotate/scale gizmos and snapping | Mid-stage | Native geometry/picking and source FK/IK callbacks are implemented. Original guide parity is bounded to the coordinate conversion paths described below. No complete visible UI interaction regression suite is present. | ST-T01, ST-T05 |
| ST-D05 | Prototype pose presets, finger gestures and analytic IK | Infancy | `Poses.swift` supplies authored native presets/gestures; `Scene/IK.swift` supplies two-bone and CCD utilities. These are useful prototype behaviors, not recovered Koikatsu pose/hand catalog behavior. | ST-T07 |
| ST-D06 | Native timeline playback and scrubbing | Infancy | Keyframes store object transform, prototype `PoseDelta`, prototype IK, expression and gesture/preset fields. They do not store source FK/IK/animation state. The comment says linear interpolation, but `Timeline.sample` uses `smoothstep`; pose translations/scales and pole choices are held from the first frame. No original Timeline plugin import/export, interpolable registry, curves or callbacks. | ST-T09 |
| ST-D07 | Screenshot and deterministic capture inputs | Mid-stage | Native captures support configured dimensions, no-grid/no-gizmo frames, source animation time and explicit object/FK/IK edit fixtures. Existing app capture proves selected output paths; it does not prove UI accessibility or full original color parity. | ST-T01, ST-T16 |
| ST-D08 | Scene CPU evaluation benchmark | Mid-stage | `benchmarkSourceEvaluation` isolates a document, advances animation/configured dynamics/plugins, constructs frames, warms three frames and reports percentiles. GPU execution, cold loads, PNG work and display scheduling are excluded. Native adapters are intentionally not mounted by the isolated benchmark. | ST-T16 |

### Original binary scene import and edited original export

Code: `KoikatsuBinary.swift`, `KoikatsuSceneRecordReader.swift`, `KoikatsuSceneRecords.swift`, `KoikatsuLayoutImporter.swift`, `SourceSceneEditing.swift`, `SourceSceneExportValidation.swift`; `StudioModel.swift` import/export methods.

| ID | Feature | Status | Specific assessment and evidence | Next task |
| --- | --- | --- | --- | --- |
| ST-S01 | Installed 1.0.4.2 writer framing and version-2 cameras | Fully ported | Bounded reader handles all observed object kinds, embedded cards, FK/IK, accessory children, routes, scene settings and trailer. Counts/lengths/nonfinite values, duplicate source keys, truncation and hierarchy depth are checked. Independent Python fixture and Swift binary/document tests verify the observed format. This rating excludes other scene versions and runtime instantiation. | ST-T03 for expansion |
| ST-S02 | Unknown/ExtendedSave scene payload preservation | Fully ported | Raw source bytes and unknown trailers survive unchanged; recognized `KKEx` MessagePack is exposed separately. Unknown/corrupt extension data remains retained with diagnostics. This rating covers retention only. | ST-T13 for execution |
| ST-S03 | Source scene character preview import | Mid-stage | Exact scene SHA and object key resolve source cards; native UUIDs remain separate. Missing assets become explicit unrendered tree nodes. Only current/slot cameras are adopted from scene settings. App preflight loads supported characters before publishing the document. | ST-T03, ST-T04, ST-T06 |
| ST-S04 | Props/folders through explicit converted layout catalog | Mid-stage | Separate `KoikatsuLayoutImporter` maps exact group/category/no to local glTF/GLB, preserves transforms/quaternion and visibility, rejects unknown kinds before returning a document. It does not apply original item colors, patterns, animation, FK/dynamics, lights or scene settings. It is not integrated into the mixed original-scene preview importer. | ST-T04 |
| ST-S05 | Bounded existing transform/FK/IK/look-at/item-FK edits | Fully ported | Pure writer patches existing byte spans, including ordinal named item bones. It rejects unknown/duplicate destinations and nonfinite values before returning bytes. No-op returns identical bytes. Tests check unrelated records and reverse-to-exact-original behavior. App exposes only a subset of these writer destinations. | ST-T03, ST-T07 |
| ST-S06 | Embedded card replacement within original scenes | Mid-stage | Engine writer can resize a card through `SourceCharacterCard.Edits` while retaining card/scene plugin payloads and checking assembly identity. App export does not populate `edits.cards`; it rejects native `object.card` overrides. Engine capability is not an end-user source-card edit workflow. | ST-T06 |
| ST-S07 | Existing kinematic flags, normal animation fields, voice playlist and camera edits | Fully ported | Byte writer handles seven FK/five IK flags, animation IDs/speed/pattern/force-loop/options/time, variable-length ordered voice list/repeat and camera/ten slots. Tests retain cards/trailers and reverse changes exactly. This does not include every character field or scene effect. | ST-T03 for remaining fields |
| ST-S08 | Edited original scene app export | Mid-stage | Source SHA checked, new filename required, supported transforms/poses/animation/voice/cameras patched, explicit edited thumbnail rendered. Fails on added/deleted/duplicated objects, reparent/visibility/name/type changes, native assets/materials/cards/hand/appearance overrides, lights/effects/timeline changes and typed plugin state. Rejection avoids silently dropping edits. | ST-T03 |
| ST-S09 | Scene creation/topology edits in original format | Pending | No original writer for insert/delete/reparent/rename/visibility, missing FK/IK destination insertion, or source object-key remapping. Keeping the source file alone does not serialize those changes. | ST-T03 |

### Character assembly, appearance and attachments

Code: `SourceStudioCharacterPreview.swift`, `SourceStudioAttachments.swift`, `SourceStudioGuide.swift`; app import, frame construction and attachment methods.

| ID | Feature | Status | Specific assessment and evidence | Next task |
| --- | --- | --- | --- | --- |
| ST-C01 | Source character assembly and selected outfit | Mid-stage | Reuses Maker assembly selection by sex/head/bone type, resolves outfit 0…6 and converted hair/clothes/accessories, applies shape contract and static ABMX. Head/bone variants are no longer universally restricted to head-00/bone-0. Coverage still depends on the converted local library and Maker's remaining destinations. | ST-T06; see Character/Maker audit |
| ST-C02 | Card material and saved expression application | Mid-stage | Card appearance bindings/material overlays and expression status fields are applied at construction. Without Maker library, reference hair/clothes are retained with a diagnostic. `prepared.appearance` receives `modLibrary: nil`; not every installed material/plugin source is available. These inputs are immutable for the preview instance. | ST-T06, ST-T07 |
| ST-C03 | Source live clothing/coordinate/accessory state editing | Pending | Source clothes tab currently only contains accessory labels and is blocked by ST-B01. Record accessory states/groups and animation option visibility are read but have no corresponding mutable native source controls. Prototype visibility booleans do not reach the source preview. | ST-T01, ST-T06 |
| ST-C04 | Saved mouth/eye/brow settings | Mid-stage | Card status selection plus saved `mouthOpen` feeds `SourceExpressionInputs`. Source Face Inspector is absent: the existing inspector requires a prototype `card`. Dynamic blink, source look-at, neck/eye serialized state, hand patterns and voice-driven mouth are not consumed by this preview. | ST-T07 |
| ST-C05 | Original accessory-point transform mapping | Fully ported | Exact source integer IDs map to original ChaReference node names, not guessed humanoid joints. Matrix/rotation read the post-animation/FK/IK/dynamics pose. Tests verify selected attachments, nonuniform parent quaternion composition and edited animation pose consistency. Scope is converted points on a uniquely bound rig. | ST-T04 for attached props; ST-T05 for wider evidence |
| ST-C06 | Hierarchical source attachments in rendering, guides and plugins | Mid-stage | App frame traversal and plugin host include attachment matrix and separate quaternion composition. Missing/ambiguous attachments reject. Mixed item/source-character children are not all instantiated by scene import, and regular tree reparenting bypasses this path. | ST-T04, ST-T15 |

### FK, full-body IK and dynamics

Code: `SourceStudioPose.swift`, `SourceStudioIK.swift`, `SourceFullBodyBiped.swift`, `SourceStudioIKEditing.swift`, `SourceStudioGuide.swift`, `SourceStudioDynamics.swift`; `Scene/SourceTrigonometricIK.swift` and `Scene/IK.swift`.

| ID | Feature | Status | Specific assessment and evidence | Next task |
| --- | --- | --- | --- | --- |
| ST-K01 | Original FK selection, activation and local-rotation write order | Fully ported | Recovered table order, depth-first scoped binding, female level-2 handling, seven groups, forced/nonreactive behavior and absolute local rotations are implemented. Both saved modes enabled resolves to IK through the original load order. `SourceStudioPoseTests` and independent activation oracle cover this bounded controller. Effects describing other consumers are not proof those consumers execute. | ST-T07 for deferred effects |
| ST-K02 | Direct rigid trigonometric source IK | Fully ported | Separate source kernel preserves source weight/order and axial-vector reflection; independent matrix oracle covers it. Scope: direct three-node rigid chain, finite unit rotations, nonzero lengths. Nonuniform/intermediate-chain/rotation-limit behavior is outside this kernel. | ST-T05 for unsupported scope |
| ST-K03 | Schema-2 five-chain full-body biped solve | Mid-stage | Body/proximal coupling, nine effectors, four cross-body constraints, chain iterations, bend/relative-effector behavior, spine FABRIK/twist, clavicle swing and head maintain-rotation execute. Positive nonuniform local scales supported; signed/sheared solver transforms reject. Twelve cases agree with unchanged recovered C# in an independent math host. Direct Unity-player full-body comparison and runtime solver hooks remain outstanding. | ST-T05 |
| ST-K04 | Thirteen source guides and missing-target initialization | Fully ported | IDs 0…12 retained; hands/feet accept rotation. Missing values initialize from original bone-copy behavior using explicit initialization pose, rather than prefab origin placeholders. Callback conversion correctly inverts scaled attached character frame. Tests retain target dictionary keys/scale/activation. Scope excludes original coroutine timing. | ST-T01, ST-T05 |
| ST-K05 | Source IK/FK editor and persistence | Mid-stage | Source callbacks, pick IDs, numerical fields, enabled/group state and native/original persistence exist. All thirteen-guide edited source roundtrip has exact local matrices and 40,490 exact deformed vertices. ST-B01 prevents the source inspector from being reached normally. | ST-T01 |
| ST-K06 | Legacy schema-1 fallback | Mid-stage | Executes bounded limb solve and explicitly reports deferred body/proximal/iterative stages. Its warning must not be interpreted as applying to schema-2. Binding regeneration is needed to obtain full-body behavior. | ST-T05 |
| ST-K07 | Selected hair DynamicBone execution order/state | Mid-stage | Stateful particles run after animation/FK/IK only when an explicit dynamics tick is supplied; repeated consumers share a result. Seek/paused-edit resets and plugin rollback checkpoints prevent double integration. Hair/skirt FK disable groups exist, but automatic binding currently accepts hair only. | ST-T08 |
| ST-K08 | DynamicBone identity/parameter recovery | Mid-stage | Exact asset IDs can rebind compacted hair slots. Current expanded contract has 15 components, 62 particles and 24 body colliders for back-0 and selected front variants. Numeric original-player setup/curve probe and independent float32 motion oracle are distinct evidence; neither is a complete original runtime animation+cloth comparison. | ST-T08 |
| ST-K09 | Cloth/accessory/body spring variants and world-motion inertia | Pending | Preview explicitly reports unimplemented unrelated cloth/accessory dynamics and object-motion inertia. DynamicBone_Ver02, virtual ends, exclusions/notRoll, distance disabling and all installed topologies are not covered by selected exporter/runtime path. | ST-T08 |

### Original Studio animation, cameras, audio, routes and effects

Code: `SourceStudioAnimation.swift`, `SourceStudioCamera.swift`, `SourceStudioVoice.swift`, `SourceStudioAudioBus.swift`, `StudioModel+Voice.swift`, parsed records/settings and app frame construction.

| ID | Feature | Status | Specific assessment and evidence | Next task |
| --- | --- | --- | --- | --- |
| ST-A01 | Normal Studio catalog-to-controller/state identity | Mid-stage | Hash-verified exact group/category/no and state/controller bindings. Current base-00 catalog: 287/288 executable rows; remaining row rejects generic loop-pose correction. All otherwise unbound tracks are classified as original low-detail-only bones for the selected high-detail rig, not guessed. Not coverage of every installed animation/mod. | ST-T10 |
| ST-A02 | Normal speed/height/normalized-time/force-loop behavior | Fully ported | Source height parameter rules and separate state/Animator speed implemented; source forced-loop overshoot discards at update boundary. Fixed 60 Hz clock with cached full-step boundaries has repeatable fractional samples/seeks and saved evaluated time. Source clock tests and actual Unity numeric probes support bounded normal states. | ST-T10 for wider controllers |
| ST-A03 | Animator transform clip and flat 1D blend application | Mid-stage | Streamed cubic, dense and constant generic Transform curves and flat 1D blend states execute. Actual Unity comparison covers five states, height/body variants, phases and speeds (see evidence). Layers, overrides, transitions, events, root motion, nested/mirrored/time-scaled motion nodes and option-item animations remain outside the converter/runtime. | ST-T10 |
| ST-A04 | Source animation selection/scrubbing UI | Infancy | State data and deterministic time setter exist; playback is sampled in frame construction and save/export bakes time. No source catalog-selection inspector is wired. App timer uses fixed 1/30 increments; original clock behavior is sampled through the deterministic 60 Hz adapter rather than matching all Unity scheduling histories. | ST-T10, ST-T16 |
| ST-A05 | Current camera and ten saved camera records | Fully ported | Source camera position includes full distance vector and Euler roll. Native orientation override retains roll; unedited byte records remain verbatim. Camera source/native roundtrip tests cover position, forward and up. Scope excludes source object-camera playback/routes/UI callbacks. | ST-T11 |
| ST-A06 | Source camera objects, switching and camera animation | Pending | Native camera objects work for native scenes, but source camera records are imported as unrendered folders. No source camera route/animation runtime. | ST-T11 |
| ST-A07 | Voice ordered playlist/repeat/parameters | Fully ported | Recovered None/All/Select scheduler, explicit play/stop, personality volume and clamped voiceRate→pitch 0.94…1.06; duplicates/order/unknown enum retained. Scene import does not auto-play. Source control, card parameter and byte-roundtrip tests cover this bounded behavior. | ST-T12 for media/full integration |
| ST-A08 | Native PCM playback, gain and cancellation | Mid-stage | AVAudioEngine per-voice, voice-group and master gain are separate; completion generations prevent stale continuation. Generated 440 Hz offline tests verify gain and mute without hardware output. Local catalog has 6,210 metadata rows and zero converted original audio files. No original waveform, spatial attenuation or audio-driven mouth parity. | ST-T12 |
| ST-A09 | Scene BGM/environment/outside sound | Pending | Scene sound IDs/file/repeat/play flags parsed and retained only; no mixer/category/playback consumers or editor in Studio. | ST-T12 |
| ST-A10 | Routes/point aids/easing/orientation | Pending | `KoikatsuRouteRecord`/point records preserve all parsed route fields. No `SourceStudioRoute` implementation exists. Import marks routes and every descendant unrendered; no tween, childRoot, play/stop/loop, guide callback or edited route serialization. | ST-T11 |
| ST-A11 | Original maps/lights/background/frame | Pending | Records preserve maps/transforms, character/map lights, background/frame names. Source preview applies only cameras. Native scene lights and gradient background are separate features. | ST-T04, ST-T11 |
| ST-A12 | Original post-processing and material-global settings | Pending | Reader retains AOE, bloom, depth, fog, shafts, vignette, color-correction/ramp, face-shadow and line settings. Native renderer controls exist but no source mapping is applied and original export rejects changed native effects/lights. Similar control names do not establish equivalent math or passes. | ST-T11; see Renderer audit |

### Studio plugin host and installed adapters

Code: `SourceStudioPluginWorld.swift`, `SourceStudioPluginSession.swift`, `SourceStudioAccessoryNamesPlugin.swift`, `StudioModel+Plugins.swift`; underlying IR/package/runtime is covered by the app/gameplay/plugins audit.

| ID | Feature | Status | Specific assessment and evidence | Next task |
| --- | --- | --- | --- | --- |
| ST-P01 | Typed source API world and attachment-aware transforms | Mid-stage | Unity basis is converted once at document boundary. Stable native/source identities, active hierarchy, subtree clone, deferred destruction/tombstones and transactions implemented. Clones reject unrepresentable world shear/reflection. External editor topology changes require plugin reload. It is a bounded Unity API subset. | ST-T13, ST-T15 |
| ST-P02 | Plugin session state, native save/reload and animation seam | Mid-stage | Profile/package hashes, field state, lifecycle clocks, original bindings, runtime clones and tombstones persist. Reload remains paused. Update→animation→LateUpdate seam rolls back clock and dynamics/Animator checkpoints on failure. Actual IR fixture save/reload/continuation evidence exists. Arbitrary assemblies/ExtendedSave callbacks do not execute. | ST-T13 |
| ST-P03 | Native installed MuteInBackground adapter mount | Mid-stage | Source adapter semantics/config parsing have tests; app connects focus to master audio gain and saves immutable native package references. The ST-B02 trap is fixed in code: `SourceApplicationFocusHost` defers the initial focus sample until launch when `NSApp` is nil, and each mount owns one observer pair. Headless captures can inject focus events and write an adapter report with generated-tone gain. A unit test covers the deferral/replacement state machine. Release run/save/reload/continue and enabled/disabled tone checks passed in PR #2, with actual GUI launch/focus smoke verification. | ST-T02 completed for this bounded acceptance; general teardown/live config remain separate |
| ST-P04 | Native StudioAccessoryNames label behavior | Fully ported | Bounded recovered coroutine behavior: UTF-16 digit detection, slot counting, label/layout substitution, no card/slot identity writes. Unit tests compare recovered source cases. This rating applies to label function only. | ST-T01 for blocked UI |
| ST-P05 | Mounted accessory-name adapter visible UI | Mid-stage | Disabled by default, enabled only after verified native package mount; source label function resolves converted accessory selections. Actual source clothes view is blocked by ST-B01. | ST-T01 |
| ST-P06 | Full scene plugin load/import/save adapters | Pending | Original KKEx map stays byte-preserved; no general ExtendedSave callback dispatch into translated plugin instances, Unity coroutine/Harmony runtime, material/editor plugins or Timeline adapters. Typed runtime state is rejected by original export rather than injected into unrelated KKEx keys. | ST-T13 |

## Recovery and validation tool inventory

| Tools/component | Status | Specific scope, evidence and actionable continuation |
| --- | --- | --- |
| `analysis/decompile_studio.py` | Mid-stage | ILSpy recovers a selected list of Studio classes and source hashes into ignored output. Expand missing runtime classes through explicit source dependency inventory; decompilation alone is not a completed port. |
| `analysis/studio_scene_contract.py`, `test_studio_scene_contract.py`, `analysis/scene_editing_oracle.py` | Mid-stage | Independent binary writer/reader/offset fixture and edited-file audits cover installed framing and bounded edit preservation. Add every new destination/topology branch before ST-T03 is complete. |
| `analysis/studio_pose_contract.py`, `test_studio_pose_contract.py` | Mid-stage | Source bone tables and independent activation/FK fixture. Add missing source consumers and compare events as part of ST-T07. |
| `analysis/studio_ik_bindings.py`, `analysis/studio_fullbody_oracle.py`, `fixtures/FinalIKOracle/*` | Mid-stage | Schema-2 topology and original hashes extracted; unchanged recovered C# runs in a purpose-built Unity math host. No direct original solver DLL/player run. Extend to actual player trace in ST-T05. |
| `analysis/trigonometric_ik_contract.py`, `test_trigonometric_ik_contract.py` | Fully ported | Bounded direct rigid-chain mathematical oracle/tool and source provenance support ST-K02. It does not supply full-body Unity engine execution. |
| `studio_attachments.py` | Mid-stage | Exact Studio attachment table export; native selection coverage tested. Add all installed tables/variants with exact source IDs and mixed-item integration in ST-T04. |
| `animation_assets.py`, `studio_animation.py`, their tests, `analysis/studio_animation_contract.py` | Mid-stage | Generic Transform clips and selected normal states plus binary32 clocks. Reports unsupported states instead of silently marking them executable. Implement loop-pose correction and wider controller types in ST-T10. |
| `original_animation_probe.py`, `fixtures/OriginalAnimationProbe.cs`, `analysis/original_animation_reference.py` | Mid-stage | Controlled private original Unity run produces numeric local matrices only; current five-state coverage measured below. Add source-mode histories/other rig families rather than interpreting these samples as all-Animator parity. |
| `dynamics_contract.py`, `test_dynamics_contract.py`, `analysis/dynamics_reference.py`, `analysis/test_dynamics_reference.py` | Mid-stage | Selected real-node hair extraction, curve recovery and independent float32 motion/collider oracle. Explicitly rejects unsupported topology; expand remaining families in ST-T08. |
| `original_dynamics_probe.py`, `fixtures/OriginalDynamicsProbe.cs`, `compare_dynamics_probe.py`, its test | Mid-stage | Numeric original-player component setup and distribution-curve comparison; not a captured original particle-motion sequence. Add runtime motion/collision capture in ST-T08. |
| `studio_selection_fixture.py`, `studio_dynamics_fixture.py` | Mid-stage | Reproducible explicitly authored clothed selection/dynamics fixtures. Expand sex/head/bone/attachment/scene combinations for ST-T16. |
| `studio_voice.py` | Mid-stage | Catalog/personality/config metadata plus explicitly selected audio conversion capability. Current inventory contains metadata, not converted original waveforms. ST-T12 needs converted-file coverage and controlled playback checks. |
| `analysis/animation_playback_contract.py`, `analysis/test_animation_playback_contract.py` | Mid-stage | Independent expression timing/random-draw oracle exists. Source Studio preview does not yet wire the complete expression/look-at/voice sequence; use it in ST-T07. |

## Evidence and its limits

The test families reviewed include `KoikatsuBinaryTests`, `KoikatsuSceneDocumentTests`, `KoikatsuLayoutTests`, `StudioHierarchyTests`, `SourceSceneEditingTests`, `SourceSceneExportValidationTests`, all `SourceStudio*Tests`, `SourceFullBodyBipedTests`, `SourceTrigonometricIKTests`, `SourceDynamicBone*Tests`, and `SourceOriginalAnimatorTests`. Many original-asset tests **return early when environment variables are absent**. A green default Swift run is not evidence those source cases ran; preserve the validation environment and artifact hashes in follow-up work.

| Existing artifact | Observed result | What it establishes / does not establish |
| --- | --- | --- |
| `.local/reverse/studio-fullbody/native-parity.json` | 12 cases; maximum world position error 9.36e-6, solver-node error 1.17e-5, quaternion-dot error 1.20e-7 | Native vs unchanged recovered C# with independent math host. Not original Unity binary/runtime full-body parity. |
| `.local/reverse/studio-expansion/ik-roundtrip-audit.json` | 13 edited guides; 40,490 vertices; matrix/vertex error 0; card and plugin trailer byte-identical | Selected source rig edit→original export→native reload fidelity. Not arbitrary scene/rig coverage or proof that the inspector is reachable. |
| `.local/reverse/original-animation-probe/native-comparison.json` | 180 cases, 60,804 local matrices; max matrix error 3.1143427e-5; max clock error 5.9604645e-8 | Actual Unity 5.6.2 Animator numeric behavior for five normal states, three heights, phases and speeds. Excludes downstream full-body/dynamics and pixels. |
| `.local/reverse/studio-animation/library/catalog.json` | 287 executable rows / 288 base normal rows | Selected catalog conversion; remaining row explicitly reports generic loop-pose correction unsupported. Do not call every installed controller converted. |
| `docs/reference/animation/dynamics-parity.md` and selected local dynamics reports | Independent original-hierarchy float32 reference: 100 frames, exact particle states; applied world position error ≤2.38e-7 | Independent motion arithmetic reference, separate from original-player parameter/curve capture; no direct original skinned hair motion comparison. |
| `.local/reverse/plugin-execution/release-capture-01/report.json` | Typed IR fixture save/reload pixel-identical; Start remains 1; continuation reaches elapsed ~2 s with original key/native UUID retained | App IR execution/persistence succeeds for fixture. It does **not** test installed native Mute adapter mount. |
| `.local/reverse/plugin-execution/native-adapter-capture-01` | Release app crashed at startup (`NSApp` nil) | Confirms ST-B02; adapter unit/package tests do not close integration. Predates the deferred-focus fix; rerun as ST-T02 describes. |
| Release fixture CPU/GPU benchmark in `release-capture-01` | Apple M2 Ultra, 600×800, 28 items / 60,761 triangles; CPU scene p50 4.684 ms / p95 4.964 ms; GPU p50 0.529 ms; RSS ~379 MB / reported Metal allocations ~416 MB | One warm fixture, separate CPU/GPU measures. Not all-scene FPS, no cold-load benchmark, and unified-memory figures overlap. See renderer evidence for matched frames. |

## Actionable pending work

Task IDs are stable handoff references. P0 blocks a current exposed workflow, P1 completes the principal source Studio path, P2 broadens behavior/coverage. Acceptance criteria distinguish source byte retention from behavior.

| Task | Priority | Implementation work | Acceptance criteria |
| --- | --- | --- | --- |
| **ST-T01** | P0 | Replace stale source-tab interception with capability-specific views. Make source pose inspector reachable; surface label adapter; gate unimplemented source appearance controls accurately. Remove/update obsolete full-body status text. | In the real app, select a loaded source character, enable IK, select/edit all 13 guides, undo/redo, save/reload and export original. Source Clothes shows mounted labels; unsupported face/clothing actions cannot silently write unused prototype fields. Record a visible UI interaction result as well as engine tests. |
| **ST-T02** | Completed P0 | Move initial Mute focus sampling until NSApplication exists or safely defer it; keep package/config identities and observed callback ordering. Ensure remount/teardown leaves one observer set. **Implemented and locally verified in PR #2:** `mountSourceFocusAdapter` owns the only observer pair and replaces or removes it on remount/unmount. The replaced adapter is restored before the new one receives its initial sample. Without `NSApp`, that sample is delivered once at `didFinishLaunching`, and a real focus change supersedes it. `IKKOKU_APPLICATION_FOCUS` and `IKKOKU_NATIVE_PLUGIN_REPORT` provide headless focus and gain evidence. | Repeat failed `native-adapter-capture-01` release launch with both native package mounts, save/reload, focus loss/gain and generated-tone gain verification. No SIGTRAP; same package hashes/settings; no duplicate observers. Use the probe command in [native adapters](../reference/mods/native-adapters.md#startup-and-acceptance); it checks each of these in run/reload/continue. Completed evidence: `.local/reverse/plugin-execution/accepted-pr-2/{installed-config,enabled-config}/report.json` and `gui-smoke.json`. Both branches retain original identities; enabled gain/RMS restore and reload pixels are identical. See [acceptance](../reference/mods/native-adapters.md#verified-release-acceptance). |
| **ST-T03** | P1 | Extend original edit model/writer incrementally: source-card app edits, name/visibility, object insertion/deletion/reparenting, missing FK/IK records, remaining source fields. Preserve original dictionary and plugin identities. | Each new destination has an independent binary-reader assertion, no-op byte identity and edit/revert test. Reload controlled exported scene in original CharaStudio and native app with equal intended state. Unsupported edits continue to reject atomically. |
| **ST-T04** | P1 | Unify mixed source importer with exact prop/map/light/object-camera asset catalogs and source attachment hierarchy. Recover item material/pattern/FK/dynamic consumers. | Controlled mixed scene renders all declared supported kinds, nested accessory attachments follow animated/FK/IK pose, item IDs/material bindings remain source identities; missing content reported by exact catalog key. No placeholder reported as rendered coverage. |
| **ST-T05** | P1 | Capture unchanged original Unity full-body solver frames including initialization delay/history; compare complete source/native poses and guide callbacks. Decide explicit support for dynamic solver settings, hook phases and signed/sheared transforms; regenerate schema-1 bindings where possible. | Original-player trace covers body/proximal/distal edits, nonuniform customization, iterations, disabled groups and attachment parents. Publish tolerances/max errors and retained exclusions. Visible editor output uses schema-2 or clearly reports fallback. |
| **ST-T06** | P1 | Add mutable source appearance/card edit state to Studio, reusing Maker identity-preserving edits and conversion resolution. Handle coordinate/clothing/accessory groups and integrate card bytes in original scene export. | An authored clothed male/female variant fixture changes selected outfit/material/accessory state, renders intended converted parts, saves native, exports original and reloads consistently. UAR/ABMX/KKEx and unrelated card bytes remain intact. Missing conversions cannot substitute unrelated IDs. |
| **ST-T07** | P1 | Recover/wire source hands, expression scheduling/blink, neck/eyes serialized state, look-at and lip-sync consumers. Route saved flags and deferred FK activation effects to their actual native systems. | Numeric original traces cover source hand patterns, expression transitions, neck/eye target/restore and voice timing. Source Face/Pose controls act on source fields; frame order and edited original persistence are tested. |
| **ST-T08** | P1 | Expand dynamics extraction/runtime to selected cloth/accessory/body variants, DynamicBone_Ver02 and world owner-motion inertia; handle virtual ends/exclusions/notRoll/distance rules explicitly. | Capture actual original particle/transform motion and compare controlled sequences after animation and IK, including FK disable, reenable, seek and rollback. Coverage report lists component/particle/collider counts per asset, rejected topologies and measured vertex/rotation errors. |
| **ST-T09** | P2 | Implement source-aware timeline tracks and interpolation first; then recover original Timeline plugin serializer/interpolable callbacks and identity binding. | Source FK/IK/animation/voice/camera tracks scrub and loop deterministically, save/reload without type/ID loss, and match an authored original Timeline scene. Tests distinguish smoothstep/linear/custom curves and discrete event behavior. |
| **ST-T10** | P1 | Finish generic loop-pose correction, then controller transitions/layers/overrides/events/root motion and animation option items. Add source catalog selection UI and exact playback parameter/state controls. | All 288 base normal rows convert or remain explicitly excluded with measured reason; additional controller families have source-vs-native numeric traces. Test state switches, blends, saved phase/speed and original export without altering catalog IDs. |
| **ST-T11** | P1 | Recover `OCIRoute.SetPath/Play/Stop`, `StudioTween`, route-point guide callbacks and childRoot placement; add route runtime/serialization. Implement source camera objects, scene map/light/background/frame and effects mapping using recovered behavior. | Neutral route fixture matches original position/orientation/easing/loop/stop at sampled times and renders its children. Camera roll/slot behavior remains correct. Lighting/effects compare matched original frames and edited original scenes reopen with same settings. |
| **ST-T12** | P2 | Convert explicitly selected voice files and add spatial audio/voice-driven mouth. Implement source scene BGM/environment/outside channels and repeat/state controls. | Exact catalog IDs resolve hash-verified files; generated audio tests cover gain/pitch/cancellation; controlled source comparison measures timing. Scene import honors original start/stop semantics and missing conversions report exact keys. |
| **ST-T13** | P1 | Add source scene load/import/save plugin adapters and necessary host APIs; implement transactional editor topology reconciliation for supported mutations. Keep native runtime state separate until a compatible original plugin schema is recovered. | A named supported plugin executes its original callbacks against controlled scene data, preserves GUID/version/KKEx payload, survives save/reload without repeated Start or identity reassignment, and rejects unsupported hooks explicitly. |
| **ST-T15** | P2 | Harden native/source tree operations, using attachment-aware frames for reparenting and explicit policies for clone/source keys, shear, plugin bindings and undo state. | Tests cover attached child under nonuniform parents, subtree duplicate, source-key uniqueness, plugin-bound scene edits, undo/redo and cycle/singular failures. World placement preserved within tolerance or operation rejected before mutation. |
| **ST-T16** | P2 | Add representative multi-character/mixed scenes, cold-load/memory-growth runs and measured app scheduling. Keep CPU evaluation, GPU/render, readback and display time separate; expose asset/effect coverage with every result. | Reproducible Release benchmark reports p50/p95, hardware/size/hash, loaded/missing assets, settled memory and repeated-load behavior. Matched original frames use identical camera/time/light/visibility, with geometry/depth/normals and full color metrics reported separately. |

## Comments and documentation requiring correction during follow-up

- `StudioView` source-tab text is a functional gate and stale documentation (ST-B01), while `docs/reference/studio/full-body-ik.md` currently says the source pose inspector exposes the guides without mentioning the gate. Amend both together when ST-T01 is implemented.
- `KoikatsuCharacterRecord.makePose` says source IK is not executed. This is locally true for that FK controller helper, but a caller can execute `SourceStudioIK` afterward; `SourceStudioCharacterPreview` already filters that diagnostic when native IK binds. Keep helper comments scoped rather than using them as whole-app status.
- `SourceStudioPose.setPVEnabled` produces effects, not an executed animation-to-pole callback. Recovery of a method or enum does not imply a connected consumer.
- `Timeline.swift` calls its interpolation linear although the implementation uses eased `smoothstep`. Its similarity to the original Timeline plugin is descriptive, not a compatibility claim.
- The schema-1 `SourceStudioIK` deferred-body warning remains correct for schema-1 only. It should remain separate from schema-2 full-body coverage.
- Update old checkpoint prose using this source/UI/evidence distinction; numerical tests, visible app workflows and original file reopening are separate completion gates.
